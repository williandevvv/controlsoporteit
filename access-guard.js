import{initializeApp,getApps,getApp}from'https://www.gstatic.com/firebasejs/10.14.1/firebase-app.js';import{getAuth,onAuthStateChanged}from'https://www.gstatic.com/firebasejs/10.14.1/firebase-auth.js';import{getFirestore,getDoc,doc}from'https://www.gstatic.com/firebasejs/10.14.1/firebase-firestore.js';import{firebaseConfig}from'./config.js';
const app=getApps().length?getApp():initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app);let profile=null;
const map={dashboard:'dashboard',tasks:'tasks',incidents:'incidents',inventory:'inventory',personnel:'personnel',pending:'pending',timeRecords:'timeRecords',reports:'reports',dailyReports:'reports',formatos:'formatos',jefatura:'executiveReport',supervision:'supervision',reminders:'reminders',spotify:'spotify',alerts:'alerts',productivity:'productivity',profile:'employeeProfile',agenda:'agenda',minutes:'minutes',audit:'audit',history:'history',assign:'assign'};
async function refresh(u){profile=null;if(!u)return;try{const s=await getDoc(doc(db,'users',u.uid));profile=s.data()||{}}catch{}}
const allowed=k=>profile?.role==='ADMIN'||profile?.permissions?.[k]===true;
document.addEventListener('click',e=>{const el=e.target.closest('[data-view],[data-side-tool],[data-gestion-nav],[data-formatos-nav],[data-jefatura-nav],[data-history-nav]');if(!el)return;const k=map[el.dataset.view]||map[el.dataset.sideTool]||map[el.dataset.gestionNav]||(el.matches('[data-formatos-nav]')?'formatos':el.matches('[data-jefatura-nav]')?'executiveReport':el.matches('[data-history-nav]')?'history':null);if(k&&!allowed(k)){e.preventDefault();e.stopImmediatePropagation();alert('Este apartado está bloqueado para tu usuario. Pedile al administrador que te habilite el permiso.');return}});
onAuthStateChanged(auth,refresh);